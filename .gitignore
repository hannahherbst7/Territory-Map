# macOS
.DS_Store

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# scratch / preview files
_*.html

# Claude Code launch config (machine-specific paths)
.claude/launch.json
